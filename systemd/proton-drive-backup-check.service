[Unit]
Description=Check that a Proton Drive backup succeeded recently
# If both catch-ups fire together at session start, the backup goes first. The
# script revalidates the unit's state anyway.
After=proton-drive-backup.service

[Service]
Type=oneshot
ExecStart=%h/bin/proton-drive-backup-check.sh
TimeoutStartSec=120
